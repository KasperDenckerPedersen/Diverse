# Add comments to this code to explain how it works and predict what it will do.

def location(country): # Define function with one argument (country)
    print(f"I am from {country}")

location("Germany") # Call the function and parse the string "Germany" as argument for country
