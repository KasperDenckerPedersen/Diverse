# Add comments to this code to explain how it works and predict what it will do.

score = 0 # Assign the integer 0 to the variable score
print(f"Score: {score}") # Print the score (0)

score += 3 # Increment the value of score by 3
print(f"Score: {score}") # Print the score (3)

score -= 1 # Decrement the value of score by 1
print(f"Score: {score}") # Print the score (2)

score = 5 # Assign the integer 5 to the variable score
print(f"Score: {score}") # Print the score (5)