# Add comments to the code below to explain what it does

firstName = "Homer" # Assign the string "Homer" to the variable firstName

lastName = "Simpson" # Assign the string "Simpson" to the variable lastName

fullName = firstName + " " + lastName # Concatenate the strings in the two variables and assign the resulting string to the variable fullName

print(fullName) # Output the value of the variable fullName to the console