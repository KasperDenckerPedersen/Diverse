# Add comments to this code to explain how it works and predict what it will do.

fileHandler = open('03_Files/mydata.txt') # Open connection to the file mydata.txt

for line in fileHandler: # Iterate over the file line by line
  print(line) # Print the line